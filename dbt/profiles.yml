ukpolice_project:
  target: dev
  outputs:
    dev:
      type: clickhouse
      host: clickhouse-server
      port: 8123
      user: "{{ env_var('CLICKHOUSE_USER') }}"
      password: "{{ env_var('CLICKHOUSE_PASSWORD') }}"
      schema: ukpolice
      secure: false
      threads: 4
